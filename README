# A full screen video player on top of [react-native-video](https://github.com/react-native-community/react-native-video/)

Import component:
```
import FullScreenPlayer from 'fullscreenplayer';
```

Use component:
```
<FullScreenPlayer {src: "<video-url>"} />
```
